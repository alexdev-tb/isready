# isready

**isready** is a cli tool that helps developers speed up the process of checking if their services are up and ready for work.

**isready** uses a config file named "isready.toml" which is simple and fast to configure.
